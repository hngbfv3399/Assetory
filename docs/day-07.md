# 7일차 작업일지

## 오늘의 목표

- 중간점검 결과를 기준으로 Notion 개발 일정과 세부 단계 문서를 실제 구현 상태에 맞춘다.
- 이후 구현 순서를 확정한다.

## 완료한 작업

- [x] Notion 개발 일정의 1~5단계 상태·진행률·비고를 코드와 검증 기록에 맞춰 갱신했다.
- [x] Notion 개발 일정의 1~10단계 상세 템플릿에 완료 범위, 남은 TODO, 다음 단계 의존성, 핵심 검증을 기록했다.
- [x] Notion API 설계의 1~5단계 엔드포인트를 현재 Controller와 대조했다. 경로·HTTP Method·인증 조건은 일치한다.
- [x] 구현 순서를 모든 백엔드 도메인·API 완성 후 React UI 일괄 구현으로 확정하고 Notion·진행 문서에 기록했다.
- [x] Notion 주문 API 설계의 판매자별 주문 분리 응답과 Mock 결제 주문 식별자 누락을 최소 보완했다.
- [x] 주문·주문 항목·Mock 결제·구매 접근권한 기반과 바로 구매 API를 구현했다.
- [x] `curl`로 주문 생성, Mock 결제 성공, 구매 자료 목록 접근, 중복 결제 차단을 실제 서버에서 검증했다.
- [x] 서버 재시작 후 `curl`로 내 주문 목록, 본인 결제 결과, 결제 완료 구매자의 자료 목록 조회를 재검증했다.
- [x] `LINK` 구매 자료가 있는 판매자 테스트 상품을 만들고, 장바구니 주문·결제 성공 뒤 장바구니 항목 자동 제거를 `curl`로 검증했다.
- [x] 결제 완료 구매자의 자료 목록 조회와 `LINK` 외부 URL 반환을 `curl`로 검증했다.
- [x] 주문 상세, 구매 상품 목록, 구매 상품 상세 API를 구현하고 서버 재시작 뒤 `curl`로 검증했다.
- [x] FILE 자료는 권한 검증 뒤 URL로 `302` 리다이렉트하고, LINK 자료 다운로드는 차단하도록 구현·`curl` 검증했다.
- [x] 타인 구매 자료 접근, 본인 상품 구매, 재구매, Mock 결제 실패와 구매 접근권한 미생성을 `curl`로 검증했다.
- [x] 6단계 구현·오류 수정·실제 API 검증을 모두 마쳐 완료 상태로 정리했다.
- [x] 7단계 판매 주문 관리(목록·상세·상태별 건수)와 후기(작성·수정·삭제·내 목록)를 구현하고 실제 API 검증했다.
- [x] 구매자 환불 요청·취소를 구현하고 요청·중복·소유권·취소를 실제 API로 검증했다.
- [ ] 판매자 환불 승인·거절·완료, 환불 목록·상세, 환불 완료 권한 회수
- [ ] 상품 문의 REST 채팅 전체와 종단 검증

## 설계 및 진행 결정

- 2·4·5단계의 UI 미구현은 누락이 아니라 백엔드 우선 구현 전략에 따른 보류 항목이다.
- 주문·Mock 결제부터 이후 백엔드 단계를 진행하고, 모든 백엔드 완료 뒤 2~9단계 UI를 연결한 후 통합·배포를 진행한다.

## 다음 작업

- 7단계 판매 주문 관리부터 구현한다. 후기, 환불, REST 문의 채팅은 판매 주문 관리 검증 후 순서대로 진행한다.
